# ------------------------------------------------------------------------------
# Tmux Configuration (trial — not yet finalized, see tmux experiment plan)
# ------------------------------------------------------------------------------

# --- Mouse ---
set -g mouse on

# --- History ---
set -g history-limit 10000

# --- Indexing ---
set -g base-index 1
setw -g pane-base-index 1
